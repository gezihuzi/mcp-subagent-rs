[package]
name = "mcp-subagent"
version = "0.1.0"
edition = "2021"
repository = "https://github.com/gezihuzi/mcp-subagent-rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
toml = "0.8"
walkdir = "2"

[dev-dependencies]
tempfile = "3"
